#include "main.h"
/**
*mul - used to multiply 2 integers
* @a: first parameter or argument
* @b: second parameter or argument
*Return: returns the result of multiplication of 2 integers
*/
int mul(int a, int b)
{
	int mul;

	mul = a * b;

	return (mul);
}
